<article class="post">
    <h2>
        <a href="{{ path('blog_post', {slug: post.slug}) }}">
            {{ post.title }}
        </a>
    </h2>

    <p class="post-metadata">

    </p>

    <p>{{ post.summary }}</p>

    {{ include('blog/_post_tags.html.twig') }}
</article>
